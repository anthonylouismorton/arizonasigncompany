import React from "react";

export const metadata = {
  title: 'Signs',
  description: 'Signs',
  keywords: 'signs, sign manufacturing, billboards, led boards, estimate, quote'
}

export default function Signs({ children }) {
  return (
      <section>
        {children}
      </section>
  )
}
