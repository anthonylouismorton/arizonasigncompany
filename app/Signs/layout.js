import React from "react";

export const metadata = {
  title: 'Signs',
  description: 'Signs',
  keywords: 'signs, sign manufacturing, billboards, led boards, sign repair, quote'
}

export default function Signs({ children }) {
  return (
      <section>
        {children}
      </section>
  )
}
