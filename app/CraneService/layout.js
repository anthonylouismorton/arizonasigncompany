import React from "react";

export const metadata = {
  title: 'Crane Service',
  description: 'Crane Service',
  keywords: 'crane service, crane, service'
}

export default function CraneServiceLayout({ children }) {
  return (
      <section>
        {children}
      </section>
  )
}
