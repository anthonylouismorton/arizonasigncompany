import React from "react";

export const metadata = {
  title: 'Installation',
  description: 'Sign Installation',
  keywords: 'sign installation'
}

export default function Installation({ children }) {
  return (
      <section>
        {children}
      </section>
  )
}
